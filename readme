for lam:

    make sure you have the most recent version of the project before you edit files
    by running:
        git pull origin master

    to update git repo after you change files:
        cd to phase_extraction (topmost directory of project)
        git add *
        git commit -m "put your commit message here"
        git push origin master
